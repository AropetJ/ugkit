{
  "name": "ugkit",
  "private": true,
  "license": "MIT",
  "description": "Tiny, fast, well-typed libraries for building Ugandan software. The missing standard library for UG products.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
